name: Build MusicMod

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
      - name: Build
        run: cd musicchat && ./gradlew build
      - name: Upload JAR
        uses: actions/upload-artifact@v4
        with:
          name: musicmod
          path: musicchat/build/libs/*.jar
